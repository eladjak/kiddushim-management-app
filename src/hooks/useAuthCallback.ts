
/**
 * Re-export the auth callback hook from its new location
 * This maintains backward compatibility while moving to a more organized structure
 */
export { useAuthCallback } from './auth-callback/useAuthCallback';
