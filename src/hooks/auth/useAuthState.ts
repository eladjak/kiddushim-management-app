
/**
 * Re-export the auth state hook from its new location
 * This maintains backward compatibility while moving to a more organized structure
 */
export { useAuthState } from './auth-state/useAuthState';
